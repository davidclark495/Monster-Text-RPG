misc errors

BattleGame
- delay()
	- inputs will still be recognized after the delay, can cause problems
	- currently deactivated
- requires pressing enter multiple times






misc features
- Pokemon
	- add exp., level
	- add atk., def. (requires interaction w/ Attack.class)
	- add type-relationships (requires interaction w/ Attack.class, PkType.enum)
	- add PP for each move (requires interaction w/ Attack.class, BattleGame.class)
- BattleGame
	- change framework, allow Trainer vs Trainer battles
- MainGame
	- allow shifting between different activities: battle, shop, etc.